import { Repository } from 'typeorm';
import { DataBuilderTask, DataBuilderTaskBatch } from '../entities/data-builder-task.entity';
import { DataBuilderConnectionCryptoService } from './data-builder-connection-crypto.service';
import type { MysqlConnDraft } from './manifest-draft.service';
export declare class DataBuilderTaskShadowStoreService {
    private readonly taskRepo;
    private readonly batchRepo;
    private readonly cryptoService;
    constructor(taskRepo: Repository<DataBuilderTask>, batchRepo: Repository<DataBuilderTaskBatch>, cryptoService: DataBuilderConnectionCryptoService);
    enabled(): boolean;
    mirrorCreateResponse(upstream: unknown, mysql: MysqlConnDraft, fallbackManifest: Record<string, unknown>): Promise<void>;
    mirrorTaskDetail(detail: unknown): Promise<Record<string, unknown> | null>;
    mirrorTaskList(details: unknown): Promise<Array<Record<string, unknown>>>;
    mirrorExecuteBatchResponse(response: unknown): Promise<void>;
    mirrorCleanupResponse(response: unknown): Promise<void>;
    mirrorExecuteBatchFailure(taskId: string, batchIndex: number | null, errorResponse: unknown, httpStatus?: number): Promise<void>;
    mirrorCleanupFailure(taskId: string, errorResponse: unknown, httpStatus?: number): Promise<void>;
    listTaskDetails(limit?: number): Promise<Array<Record<string, unknown>>>;
    getTaskDetail(taskId: string): Promise<Record<string, unknown> | null>;
    private ensureBatchRows;
    private applyBatchAggregateDelta;
    private applyDerivedCleanupState;
    private normalizeCompletedBatches;
    private normalizeCurrentBatchIndex;
    private resolveMirroredCleanupState;
    private deriveBatchCount;
    private deriveManifestVersion;
    private normalizeErrorEnvelope;
    private shouldMarkExecutionFailed;
    private hashJson;
    private coerceStatus;
    private coerceCleanupStatus;
    private asRecord;
    private asRecordArray;
    private asString;
    private asNumber;
    private asNullableNumber;
    private asDate;
}
